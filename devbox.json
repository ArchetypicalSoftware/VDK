{
  "$schema": "https://raw.githubusercontent.com/jetify-com/devbox/0.12.0/.schema/devbox.schema.json",
  "packages": [
    "kind@0.23.0",
    "fluxcd@latest",
    "kubectl@latest",
    "k9s@latest"
  ],
  "shell": {
    "init_hook": [
      "export PATH=$PATH:~/.vega/bin && mkdir -p ~/.vega/bin"
    ],
    "scripts": {
      "test": [
        "echo \"Error: no test specified\" && exit 1"
      ]
    }
  }
}
